# Prompt — Suite de la structuration du workspace

Le workspace `Claude/` suit l'architecture définie dans `FolderSystemInstructions.md`.

L'**evolution-001** a posé les fondations : `CLAUDE.md` racine (navigation automatique), domaines `shared-knowledge/` et `workflows/`, dossier `evolutions/` dans `MyBikeLab/`.

**Prochaine étape : structurer le dossier `workflows/`.**

Le workflow de code review existe déjà (`MyBikeLab/Review/SKILL.md`) et est référencé dans `workflows/README.md`. Il faut décider comment formaliser les prochains workflows et lesquels traiter en priorité.

Lire avant d'agir : `Claude/CLAUDE.md` → `Claude/workflows/README.md`.
